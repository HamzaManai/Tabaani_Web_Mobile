{% extends 'Back_Office/AdminMain.html.twig' %}

{% block title %}Edit Promotion{% endblock %}

{% block Entry %}
<div class="container">
    <div class="row">
        <div class="col-lg-6">
            <div class="card card-custom gutter-b example example-compact">
                <div class="card-header">
                    <h3 class="card-title">Edit a Promotion</h3>
                </div>

                <div class="card-body">

    {{ include('promotion/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('promotion_index') }}">back to list</a>


                </div>

            </div>
        </div>
    </div>
</div>
{% endblock %}
