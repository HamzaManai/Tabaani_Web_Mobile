{% extends 'base.html.twig' %}

{% block title %}ParticipateEvent{% endblock %}

{% block body %}
    <h1>ParticipateEvent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ participate_event.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('participate_event_index') }}">back to list</a>

    <a href="{{ path('participate_event_edit', {'id': participate_event.id}) }}">edit</a>

    {{ include('participate_event/_delete_form.html.twig') }}
{% endblock %}
