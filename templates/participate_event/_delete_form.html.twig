<form method="post" action="{{ path('participate_event_delete', {'id': participate_event.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ participate_event.id) }}">
    <button class="btn">Delete</button>
</form>
