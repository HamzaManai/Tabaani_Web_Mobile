{% extends 'base.html.twig' %}

{% block title %}CarteFidelite{% endblock %}

{% block body %}
    <h1>CarteFidelite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ carte_fidelite.id }}</td>
            </tr>
            <tr>
                <th>NbrPoint</th>
                <td>{{ carte_fidelite.nbrPoint }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('carte_fidelite_index') }}">back to list</a>

    <a href="{{ path('carte_fidelite_edit', {'id': carte_fidelite.id}) }}">edit</a>

    {{ include('carte_fidelite/_delete_form.html.twig') }}
{% endblock %}
